<%= form_with(model: fecha, local: true) do |form| %>
  <% if fecha.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fecha.errors.count, "error") %> prohibited this fecha from being saved:</h2>

      <ul>
      <% fecha.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :Hora %>
    <%= form.time_select :Hora %>
  </div>

  <div class="field">
    <%= form.label :Pregunta %>
    <%= form.text_field :Pregunta %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
