<%= form_with(model: tipos_dato, local: true) do |form| %>
  <% if tipos_dato.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipos_dato.errors.count, "error") %> prohibited this tipos_dato from being saved:</h2>

      <ul>
      <% tipos_dato.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.time_select :time %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
